# ********RoostGPT********

# Test generated by RoostGPT for test karateIntegration using AI Type Azure Open AI and AI Model roost-gpt4-32k
# 
# ROOST_METHOD_HASH=3a6f843fa1
# ROOST_METHOD_SIG_HASH=915e9904cf
# 
#    ########## Scenario ##########  
# 
# {
#   background: 'Given the base URL is "http://localhost:8080"',
#   rule: null,
#   scenario: 'Get a product by ID\r\n' +
#     '        Given there is an existing product with ID 1\r\n' +
#     '        When the client sends a GET request "/api/products/1" to get a product by its id\r\n' +
#     '        Then the response status code should be 200\r\n' +
#     '        And the response should contain the product with ID 1',
#   title: 'Get a product by ID'
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 5
      arrivalRate: 1
  environments:
    development:
      target: "http://localhost:8080"
  payload:
    fields:
      - "productID"
    path: "GetProductByID.csv"
    skipHeader: true

scenarios:
  - name: "Get a product by ID"
    flow:
      - get: 
          url: "/api/products/{{ productID }}"
          expect: 
            - statusCode: 200
            - hasProperty: productID
            - equals: 
                productID: "{{ productID }}"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
      - think: 3
